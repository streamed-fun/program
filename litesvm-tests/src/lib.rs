//! Intentionally empty: this crate exists to hold the LiteSVM integration
//! tests, which need the compiled SBF artifact (`cargo build-sbf` first) and
//! therefore live apart from the host-runnable math tests in the program
//! crate.
